package com.jjktbf.model.character;

/** Parameters that may be required by an {@link AbilityEffectType}. */
public enum AbilityEffectParameter {
    STAT,
    INTEGER,
    DECIMAL,
    MOVE_SCOPE,
    MOVE_ID,
    TECHNIQUE,
    STATUS_TYPE,
    TARGET,
    TIMING,
    DURATION,
    MAGNITUDE,
    USES,
    BATTLE_STAT
}
